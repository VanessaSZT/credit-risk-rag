# Fallback for plain-pip users — the canonical definition is pyproject.toml + uv.lock (use `uv sync --frozen`).
langchain==0.1.16
langchain-community==0.0.34
# 0.5.5+ required for Python 3.12 (chroma-hnswlib 0.7.6 ships cp312 wheels; 0.4.x pinned 0.7.3 which does not)
chromadb==0.5.5
# chromadb declares posthog>=2.4.0 with no upper bound, but its telemetry code
# uses the pre-6.x capture() signature; without this pin every Chroma call logs
# "Failed to send telemetry event" errors.
posthog==3.7.4
pypdf==4.2.0
fpdf2==2.7.9
sentence-transformers==2.7.0
